// ====================================================================
// File:    src/connection/IoBuffer.hpp | Module: connection
// Purpose: byte buffer. append, consume(n), peek, size, pending.
//          used for both inbound and outbound bytes.
// Owner:   Developer B   Deps: <string>, <vector>
// Note:    testable in isolation. < 250 lines.
// ====================================================================
