# ====================================================================
# File:    config/default.conf
# Purpose: minimal sample. one server, one port, one location.
#          root + index + GET. used by Phase 1 vertical slice.
# Owner:   Developer A
# Note:    SKELETON. directives filled when grammar is locked.
# ====================================================================
